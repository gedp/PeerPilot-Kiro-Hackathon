# Dependencias de Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Puede ser opcional, mira la nota abajo

# Archivos de Serverless
.serverless/
.webpack/           # Común si usas serverless-webpack
.build/             # Otro posible directorio de build de Serverless
.serverless-offline/ # Si usas serverless-offline

# Entornos virtuales de Python
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# Archivos de caché y compilación de Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python/
build/
develop-eggs/
dist/
eggs/
.egg-info/
parts/
sdist/
var/
*.egg

# Archivos de configuración de entorno (variables locales)
.env
.env.example # A menudo se mantiene para mostrar el formato, pero si contiene secretos, ignóralo.

# Específico de Kiro (si Kiro genera carpetas temporales o de configuración)
.kiro/              # Carpeta de configuración interna de Kiro (ejemplo)
kiro_temp/          # Directorios temporales que Kiro podría crear
*.kiro_config       # Ejemplo de archivos de configuración local de Kiro

# Específico de AWS
.aws/               # ¡Contiene credenciales de AWS, muy importante ignorar!
*.pem               # Archivos de clave privada
*.key               # Otros archivos de clave
aws-exports.js      # Configuración de Amplify (si se usa)
# Archivos temporales de credenciales o logs de AWS si Kiro los generara

# Archivos de Editor e IDE
.idea/              # IDEs de JetBrains (PyCharm, IntelliJ)
.vscode/            # Configuración de VS Code (opcional, a veces útil para compartir ajustes de equipo)
*.sublime-project
*.sublime-workspace

# Archivos generados por el sistema operativo
.DS_Store           # macOS
.Trashes            # macOS
Thumbs.db           # Windows
ehthumbs.db         # Windows

# Logs y bases de datos locales
*.log               # Archivos de log
*.sqlite3           # Bases de datos SQLite (si se usan localmente)
*.db                # Otros archivos de base de datos

# Datos locales / Salidas generadas
data/               # Si procesas archivos de datos locales grandes que no deben ser versionados
results/            # Para resultados de pruebas locales, archivos de salida no parte del código fuente
temp/               # Archivos temporales
uploads/            # Si tu aplicación maneja subidas de archivos localmente

# Archivos de despliegue y empaquetado
*.zip
*.tar.gz
