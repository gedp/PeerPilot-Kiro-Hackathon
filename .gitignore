# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python/
build/
develop-eggs/
dist/
eggs/
.egg-info/
parts/
sdist/
var/
*.egg
.env
.env.example
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Kiro specific (if Kiro generates specific temporary folders or config files)
.kiro/           # Kiro's internal configuration folder
kiro_temp/       # Any temporary directories Kiro might create
*.kiro_config    # Example if Kiro generates local config files

# AWS specific
.aws/            # Contains AWS credentials (important!)
*.pem            # Private key files
*.key            # Other key files
# If Kiro generates any temporary AWS credential files or logs
aws-exports.js   # Amplify config if Kiro uses it (less likely for this project)

# Editor and IDE specific
.idea/           # JetBrains IDEs (PyCharm, IntelliJ)
.vscode/         # VS Code settings (optional, sometimes useful to share)
*.sublime-project
*.sublime-workspace

# OS generated files
.DS_Store        # macOS
.Trashes         # macOS
Thumbs.db        # Windows
ehthumbs.db      # Windows
*.log            # Log files
*.sqlite3        # SQLite databases (if used locally)
*.db             # Other database files

# Local data / Generated outputs
data/            # If we process large local data files that shouldn't be committed
results/         # For local test results, output files that are not part of the source code
temp/            # Temporary files

# Example of files that might be generated in our S3 flow (but are not part of source)
# If Kiro creates local copies of these, they should be ignored
# articles-input/   # NO - these are in S3, if they are downloaded locally for processing, then yes.
# journal_rules.yaml # NO - this is source data
# reviewers_example.csv # NO - this is source data
