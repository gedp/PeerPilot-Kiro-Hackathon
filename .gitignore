# Node.js dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # Can be optional, see note below

# Serverless framework files
.serverless/
.webpack/           # Common if you're using serverless-webpack
.build/             # Another possible Serverless build directory
.serverless-offline/ # If you're using serverless-offline

# Python virtual environments
.venv/
env/
venv/
ENV/
env.bak/
venv.bak/

# Python compiled files and cache
__pycache__/
*.pyc
*.pyo
*.pyd
.Python/
build/
develop-eggs/
dist/
eggs/
.egg-info/
parts/
sdist/
var/
*.egg

# Environment configuration files (local variables)
.env
.env.example # Often kept to show format, but ignore if it contains secrets.

# Kiro specific (if Kiro generates specific temporary folders or config files)
.kiro/              # Kiro's internal configuration folder (example)
kiro_temp/          # Any temporary directories Kiro might create
*.kiro_config       # Example if Kiro generates local config files

# AWS specific
.aws/               # Contains AWS credentials (crucial to ignore!)
*.pem               # Private key files
*.key               # Other key files
aws-exports.js      # Amplify config (if used)
# Temporary AWS credential files or logs if Kiro were to generate them

# Editor and IDE specific
.idea/              # JetBrains IDEs (PyCharm, IntelliJ)
.vscode/            # VS Code settings (optional, sometimes useful for team settings)
*.sublime-project
*.sublime-workspace

# OS generated files
.DS_Store           # macOS
.Trashes            # macOS
Thumbs.db           # Windows
ehthumbs.db         # Windows

# Logs and local databases
*.log               # Log files
*.sqlite3           # SQLite databases (if used locally)
*.db                # Other database files

# Local data / Generated outputs
data/               # For large local data files that shouldn't be committed
results/            # For local test results, output files not part of source code
temp/               # Temporary files
uploads/            # If your application handles local file uploads

# Deployment and packaging files
*.zip
*.tar.gz
